
<div class="container">
<h1>This is all the bookings</h1>
<ul>
  <% @bookings.each do |booking| %>
    <li><%=booking.parking.title %> is booked from 👉 <strong><%=booking.start_date.strftime('%a %d %b %Y') %></strong> to <strong><%=booking.end_date.strftime('%a %d %b %Y') %></strong> <%=booking.status %></li>
  <% end %>
</ul>

</div>
